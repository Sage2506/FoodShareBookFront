export interface IPagination {
  arrows?: any
  currentPage: string,
  endIndex: number,
  endPage: number,
  pages: number[],
  pageSize: string,
  startIndex: number,
  startPage: number,
  totalItems: string,
  totalPages: number,
}